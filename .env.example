# Yellow Protocol Quick Start Configuration

# Your wallet private key (without 0x prefix)
# IMPORTANT: Never commit your actual private key to version control!
# For testing, you can generate a new wallet with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PRIVATE_KEY=your_private_key_here

# ClearNode WebSocket URL (production)
CLEARNODE_URL=wss://clearnet.yellow.com/ws

# Application name (used in authentication)
APPLICATION_NAME=yellow-quickstart
