# Completion Checklist

- Ensure .env is configured (PRIVATE_KEY, CLEARNODE_URL, APPLICATION_NAME; optional APP_PARTICIPANT_B, APP_SESSION_ASSET, APP_SESSION_AMOUNT).
- Run bun run dev and verify connection/auth/channel/balance flow.
- If changes alter behavior, update README/examples accordingly.
